## git-sync

This project uses [git-sync](https://github.com/cart0113/git-sync) to manage
external repository dependencies. Run `bin/git-sync status` to see managed
repos. Config lives in `.git-sync.yaml` (shared) and `.git-sync-private.yaml`
(personal, gitignored).
